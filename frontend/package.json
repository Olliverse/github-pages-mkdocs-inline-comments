{
  "name": "ghc-widget",
  "private": true,
  "description": "TypeScript source of the mkdocs-inline-comments widget; bundled output is committed under src/mkdocs_inline_comments/assets/.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run --environment jsdom"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
